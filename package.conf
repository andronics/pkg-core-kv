name="kv"
description="Key-Value Store - Fast CLI data storage and retrieval"
version="0.1.0"
method="stow"
collection="core"
fold=false

# Ignore documentation files
supported_os=("linux")
depends=()
requires_commands=()
ignore=(
    "README.md"
)
